#include <stdio.h>
#include <conio.h>

int main()
{
    // Relational operator > , < , >= , <= , ==
    // math 5 = 2 in C : 5 == 2

    // int age;

    // printf("Enter your age: ");
    // scanf("%d", &age);

    // if (age < 18)
    // {
    //     printf("Your are not elegible for vote");
    // }
    // else
    // {
    //     printf("Your are elegible for vote");
    // }

    // Logical Operators || --> OR , && --> AND , ! --> NOT

    if (5 == 5 && 3 == 3)
    {
        printf("Equal");
    }
    getch();
    return 0;
}
